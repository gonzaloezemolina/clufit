# @clufit/ui

Componentes reutilizables (Button, Card, Modal, Avatar, Badge — ver `docs/09-project-structure.md`).

Vacío por ahora: falta decidir cómo se comparten componentes entre `apps/mobile` (React Native) y `apps/admin` (Next.js) — por ejemplo vía React Native Web, o manteniendo implementaciones separadas por plataforma que compartan solo tokens (`@clufit/config`). Resolver esto antes de agregar el primer componente acá.
