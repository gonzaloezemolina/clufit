/**
 * Design tokens from docs/10-design-system.md.
 * Keep this file in sync with that document — it is the single source of truth.
 */
export const colors = {
  primary: "#28417F",
  secondary: "#4BAAF8",
  accent: "#C9D5EB",
  backgroundLight: "#F6F8FC",
  backgroundDark: "#0E101C",
  surface: "#FFFFFF",
  textPrimary: "#111827",
  textSecondary: "#6B7280",
} as const;

export const radius = {
  sm: 12,
  md: 18,
  lg: 24,
  xl: 32,
} as const;
